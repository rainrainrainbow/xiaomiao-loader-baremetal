# ===== WiFi OTA 额外配置 =====
# 与 sdkconfig.defaults 配合使用（SDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.defaults.wifi"）
# 该文件由 CI WiFi job 使用，也可本地手动编译 WiFi 版：
#   export SDKCONFIG_DEFAULTS="sdkconfig.defaults;sdkconfig.defaults.wifi"
#   idf.py build

# 启用 WiFi OTA 功能（链接 esp_wifi/lwip/nvs_flash，+100~160KB）
CONFIG_XIAOMIAO_ENABLE_WIFI=y

# ── 激进的体积优化 ────────────────────────────────────────
# 目标：WiFi 版 app ≤ 300KB，装进 568KB 工厂分区

# 关闭 mbedtls TLS（不用 HTTPS，仅 HTTP 裸socket）
# 注意：esp_wifi 依赖 mbedtls 做 WPA2 加密，不能完全关闭 but 可关闭 TLS 协议
CONFIG_MBEDTLS_SSL_PROTO_DTLS=n
# CONFIG_MBEDTLS_SSL_PROTO_TLS1_2 is not set
# CONFIG_MBEDTLS_SSL_PROTO_TLS1_1 is not set
# CONFIG_MBEDTLS_SSL_PROTO_TLS1 is not set
# CONFIG_MBEDTLS_TLS_ENABLED is not set
CONFIG_MBEDTLS_CERTIFICATE_BUNDLE=n
CONFIG_MBEDTLS_HAVE_TIME=n
CONFIG_MBEDTLS_HAVE_TIME_DATE=n
CONFIG_MBEDTLS_X509_CRT_PARSE_C=n
CONFIG_MBEDTLS_SSL_KEEP_PEER_CERTIFICATE=n
CONFIG_MBEDTLS_HARDWARE_AES=y
CONFIG_MBEDTLS_HARDWARE_SHA=y
CONFIG_MBEDTLS_HARDWARE_MPI=y
CONFIG_MBEDTLS_HARDWARE_GCM=y

# 最小化 lwip
CONFIG_LWIP_IPV6=n
CONFIG_LWIP_MAX_SOCKETS=4
CONFIG_LWIP_TCP_MSS=1460
CONFIG_LWIP_TCP_WND=4096
CONFIG_LWIP_TCP_SND_BUF=4096
CONFIG_LWIP_DHCP_MAX_NTP_SERVERS=0
CONFIG_LWIP_DNS_MAX_SERVERS=1
CONFIG_LWIP_IRAM_OPTIMIZATION=n
CONFIG_LWIP_EXTRA_IRAM_OPTIMIZATION=n
CONFIG_LWIP_LOOPBACK_INTERFACE=n
CONFIG_LWIP_SNTP=n
CONFIG_LWIP_NETIF_API=n
CONFIG_LWIP_TCPIP_RECVMBOX_SIZE=16
CONFIG_LWIP_IP_REASS_MAX_PBUFS=1
CONFIG_LWIP_FRAGMENT_TCP_MSS_OPTION=n
# CONFIG_LWIP_IP_FRAG is not set
# CONFIG_LWIP_IP_REASSEMBLY is not set

# 最小化 WiFi
CONFIG_ESP_WIFI_STATIC_TX_BUFFER_NUM=2
CONFIG_ESP_WIFI_DYNAMIC_TX_BUFFER_NUM=8
CONFIG_ESP_WIFI_TX_BA_WIN=2
CONFIG_ESP_WIFI_RX_BA_WIN=2
CONFIG_ESP_WIFI_STATIC_RX_BUFFER_NUM=2
CONFIG_ESP_WIFI_DYNAMIC_RX_BUFFER_NUM=8
CONFIG_ESP_WIFI_MGMT_SBUF_NUM=2
CONFIG_ESP_WIFI_IRAM_OPT=n
CONFIG_ESP_WIFI_RX_IRAM_OPT=n
# CONFIG_ESP_WIFI_ENABLE_WPA3_SAE is not set
CONFIG_ESP_WIFI_SOFTAP_SUPPORT=n
# CONFIG_ESP_WIFI_ENHANCED_LIGHT_SLEEP is not set
CONFIG_ESP_WIFI_AMPDU_RX_ENABLED=n
CONFIG_ESP_WIFI_AMPDU_TX_ENABLED=n
CONFIG_ESP_WIFI_CSI_ENABLED=n
CONFIG_ESP_WIFI_PSCAN_ENABLE=n
CONFIG_ESP_WIFI_MBEDTLS_CRYPTO=y
CONFIG_ESP_WIFI_MBEDTLS_TLS_CLIENT=n

# 最小化 nvs_flash
CONFIG_NVS_ENCRYPTION=n
CONFIG_NVS_COMPATIBLE_PRE_V4_3_ENABLE=n

# 最小化 esp_netif
CONFIG_ESP_NETIF_TCPIP_LWIP=y
# CONFIG_ESP_NETIF_LOOPBACK is not set
# CONFIG_ESP_NETIF_BRIDGE_EN is not set

# 最小化其他
CONFIG_ESP_SYSTEM_EVENT_TASK_STACK_SIZE=2048
# CONFIG_ESP_TIMER_IMPL_FRC2 is not set
# CONFIG_ESP_TIMER_SUPPORTS_ISR_DISPATCH_METHOD is not set